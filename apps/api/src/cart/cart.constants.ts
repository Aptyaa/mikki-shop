/**
 * Сколько штук одной позиции можно положить в корзину.
 *
 * Заодно верхняя граница `maxQuantity` в ответе: точный остаток по складу
 * наружу не отдаётся, покупателю нужно знать не «на складе 47», а «взять можно».
 */
export const CART_LINE_MAX = 10;

/** Сколько разных позиций бэкенд соглашается пересчитать за раз. */
export const CART_MAX_ITEMS = 50;
