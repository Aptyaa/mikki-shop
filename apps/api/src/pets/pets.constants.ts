/** Ограничения полей карточки питомца — чтобы в базу не уехал роман вместо породы. */
export const MAX_PET_NAME = 60;
export const MAX_BREED = 100;

/**
 * Границы мерок в сантиметрах.
 *
 * Не «любое положительное число»: магазин шьёт на маленьких собак, и обхват
 * груди 300 — это опечатка, а не чихуахуа. Верх взят с большим запасом, чтобы
 * не спорить с владельцем крупной собаки, который всё же зашёл за банданой.
 */
export const MIN_CM = 1;
export const MAX_CM = 200;

/** Сколько питомцев можно завести. Столько собак не бывает — это защита от скрипта. */
export const MAX_PETS = 20;
