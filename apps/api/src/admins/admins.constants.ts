/** Сколько живёт приглашение. Сутки: ссылка, которая живёт вечно, рано или поздно уезжает в чужую переписку. */
export const INVITE_TTL_MS = 24 * 60 * 60 * 1000;

/**
 * Префикс полезной нагрузки `/start`.
 *
 * Нужен, чтобы отличать приглашение от UTM-метки из рекламной ссылки: и то и
 * другое приезжает одним и тем же параметром `start`.
 */
export const INVITE_PREFIX = "admin_";

/** Длина кода в байтах до кодирования. 16 байт — 128 бит случайности. */
export const CODE_BYTES = 16;
