/**
 * Сколько живёт JWT.
 *
 * Тридцать дней: Mini App открывают редко и ненадолго, а перевыпуск токена —
 * это лишний поход в сеть на старте каждого сеанса. `initData` под рукой
 * всегда, так что просроченный токен обменивается на новый молча.
 */
export const TOKEN_TTL_SECONDS = 30 * 24 * 60 * 60;

/** Метка, которой помечены маршруты, доступные без входа. */
export const IS_PUBLIC = "mikki:public";
