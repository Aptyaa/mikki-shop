import { defineConfig } from "vitest/config";

export default defineConfig({
  test: {
    // Тесты лежат рядом с кодом, который проверяют: `catalog.service.ts` и
    // `catalog.service.test.ts` в одной папке. Отдельного дерева `test/` нет —
    // при переезде модуля тест едет вместе с ним и не теряется.
    include: ["apps/*/src/**/*.test.{ts,tsx}", "packages/*/src/**/*.test.{ts,tsx}"],
    // Всё, что здесь проверяется, — чистая логика: ни БД, ни DOM. Поэтому один
    // общий прогон на всё монорепо, без разбиения на проекты и без jsdom.
    environment: "node",
    // Корень Vite здесь — корень монорепо, значит корневой `.env` подхватится
    // в `import.meta.env` и подставит боевой хост в `BASE_URL`. Тогда тесты
    // клиента каталога падали бы у всех, кто завёл `.env` по README, и были бы
    // зелёными только в CI, где `.env` нет. Адрес API к делу не относится:
    // проверяется собранный путь с query, поэтому база фиксируется пустой.
    env: { VITE_API_URL: "" },
  },
});
