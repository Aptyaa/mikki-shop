Filter pill. Lives in a horizontal scroller that bleeds to the screen edge (negative margin + matching padding).

```jsx
<Chip selected count={24}>Свитеры</Chip>
```

Selected state is forest, not apricot — apricot is reserved for actions.
