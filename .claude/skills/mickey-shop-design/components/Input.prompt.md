Text field. 48px tall, 14px radius — fields are the one rounded-rect family in a pill-heavy system.

```jsx
<Input label="Кличка питомца" placeholder="Микки" hint="Так мы подпишем посылку" />
<Input label="Комментарий" multiline rows={3} />
```
