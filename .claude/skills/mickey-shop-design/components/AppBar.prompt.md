In-webview header. **Do not put a back arrow here on Telegram** — the client provides BackButton above the webview. On VK, pass `left={<IconButton .../>}`.

```jsx
<AppBar title="Свитеры" right={<IconButton label="Поиск"><Icon name="search"/></IconButton>} />
```
