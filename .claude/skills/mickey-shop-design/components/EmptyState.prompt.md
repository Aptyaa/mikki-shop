Empty state. Never apologise, never blame the user — say what's there and offer one route out.

```jsx
<EmptyState title="В корзине пока пусто" body="Микки уже выбрал пару свитеров — посмотрите?"
  action={<Button variant="primary">В каталог</Button>} />
```
