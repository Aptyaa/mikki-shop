Price. Always `1 490 ₽` — space as thousands separator, ₽ after, no kopecks.

```jsx
<PriceBlock price={1490} was={2190} />
<PriceBlock price={1490} was={2190} stacked align="right" />   {/* list rows */}
```

Set in **Onest semibold**, not the display face. In the paper direction a price is information in a column,
not a headline — the display face is kept for section titles so prices do not compete with them.
