Icon-only control for chrome.

```jsx
<IconButton variant="filled" label="В избранное" active><Icon name="heart"/></IconButton>
```

`filled` when it floats over product imagery; `plain` inside an app bar. `size="sm"` only when the parent already provides padding to reach 44px.
