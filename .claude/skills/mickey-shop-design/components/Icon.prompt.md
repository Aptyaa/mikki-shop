Icon glyph. Lucide is a **substitution** — the brand had no icon set of its own.

```jsx
<Icon name="shopping-bag" size={22} />
```

Sizes: 16 (inline text), 20 (default UI), 24 (tab bar, app bar). Never mix in emoji or another icon family.
